---
import JavaScriptIcon from "@/assets/javascript.svg";
import ReactIcon from "@/assets/react.svg";
import VueIcon from "@/assets/vue.svg";
import SvelteIcon from "@/assets/svelte.svg";
import SolidIcon from "@/assets/solidjs.svg";
import AngularIcon from "@/assets/angular.svg";
import WebComponentsIcon from "@/assets/webcomponents.svg";

const frameworks = [
  { icon: ReactIcon, name: "react", title: "React" },
  { icon: VueIcon, name: "vue", title: "Vue" },
  { icon: SvelteIcon, name: "svelte", title: "Svelte" },
  { icon: SolidIcon, name: "solid", title: "Solid" },
  { icon: AngularIcon, name: "angular", title: "Angular" },
  { icon: WebComponentsIcon, name: "web-components", title: "Web Components" },
  { icon: JavaScriptIcon, name: "core", title: "Vanilla" },
]
---
<section class="framework-section">
  <h2>Pick your framework</h2>
  <div class="framework-grid">
    {frameworks.map((f) => (
      <a href={`/docs/${f.name}`} title={f.title} class="framework-icon">
        <f.icon width="96" height="96" />
      </a>
    ))}
  </div>
</section>

<style>
  .framework-section {
    padding: 2rem;
    h2 {
      text-align: center;
      font-size: 1.875rem;
      line-height: calc(2.25 / 1.875);
      text-wrap: balance;
    }
  }
  .framework-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 2rem;
    padding-block: 2rem;
  }
  .framework-icon {
    filter: grayscale(100%);
    opacity: 0.6;
    transition:
      filter 0.15s cubic-bezier(0.4, 0, 0.2, 1),
      opacity 0.15s cubic-bezier(0.4, 0, 0.2, 1);
    &:hover {
      filter: grayscale(0);
      opacity: 1;
    }
  }
</style>